APP_NAME="Laravel"
APP_ENV=local
APP_KEY=base64:gIa3oJ01+lwxjslS6oVlobH2rIJCVX2gZKLpL2dzWcM=
APP_DEBUG=false
APP_URL="https://vlstrike-production-7182.up.railway.app"

APP_LOCALE="en"
APP_FALLBACK_LOCALE="en"
APP_FAKER_LOCALE="en_US"
APP_MAINTENANCE_DRIVER=file

LOG_CHANNEL=stack
LOG_LEVEL=debug
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null

BCRYPT_ROUNDS=12
PHP_CLI_SERVER_WORKERS=4

# --- DATABASE ---
DB_CONNECTION=mysql
DB_HOST=shuttle.proxy.rlwy.net
DB_PORT=11226
DB_DATABASE=railway
DB_USERNAME=root
DB_PASSWORD=ZHHFSAuOATwVimtbgCwfLwDqWfebOAUR

# --- SESSION ---
SESSION_DRIVER=database
SESSION_CONNECTION=mysql
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_DOMAIN=null
SESSION_PATH=/

# --- CACHE & QUEUE ---
QUEUE_CONNECTION=database
CACHE_STORE=database

# --- FILESYSTEM ---
FILESYSTEM_DISK=local

# --- BROADCASTING ---
BROADCAST_CONNECTION=log

# --- REDIS (si no lo usas, déjalo así) ---
REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

# --- MAIL ---
MAIL_MAILER=log
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS=hello@example.com
MAIL_FROM_NAME="${APP_NAME}"

# --- AWS (aunque no lo uses, Laravel a veces lo espera) ---
AWS_ACCESS_KEY_ID=null
AWS_SECRET_ACCESS_KEY=null
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=null
AWS_USE_PATH_STYLE_ENDPOINT=false

# --- VITE ---
VITE_APP_NAME="${APP_NAME}"

# --- OTRAS VARIABLES PERSONALIZADAS ---
RIOT_API_KEY=RGAPI-48a94868-4942-4683-8da1-e0fb8b05b5e2

# --- BUILD PARA NIXPACKS / RAILWAY ---
NIXPACKS_BUILD_CMD="
composer install --no-dev --optimize-autoloader && \
chmod -R 777 storage bootstrap/cache && \
npm install && \
npm run build && \
php artisan migrate --force && \
php artisan config:clear && \
composer dump-autoload -o && \
php artisan storage:link && \
php artisan cache:clear && \
php artisan route:clear && \
php artisan view:clear
"
